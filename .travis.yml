  language: python

  python:
    - "3.6"

  install:
    - pip install -r requirements.txt

  # command to run tests
  script:
    - pytest

  # CI 실행 완료시 메일로 알람
  notifications:
    email:
      recipients:
        - tadanmf@gmail.com